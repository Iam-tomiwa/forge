# The public origin this Forge instance is served from. Canonical tags, absolute
# Open Graph URLs, and sitemap.xml are only emitted once it is known, so set it
# before deploying. Vercel, Netlify, and Cloudflare Pages are detected
# automatically when this is unset.
NEXT_PUBLIC_SITE_URL=https://your-forge-domain.example
